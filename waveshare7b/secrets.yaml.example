# secrets.yaml.example - Copy to secrets.yaml, never commit to git
wifi_ssid: "Your WiFi SSID"
wifi_password: "Your WiFi Password"
# Generate with: python3 -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
api_encryption_key: "YOUR_32_BYTE_BASE64_KEY"
ota_password: "YOUR_OTA_PASSWORD"
